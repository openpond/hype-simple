{
  "name": "opentool-example",
  "version": "1.0.0",
  "description": "Example OpenTool project",
  "scripts": {
    "dev": "opentool dev",
    "build": "opentool build --input tools --output dist",
    "metadata": "opentool metadata --input tools --output metadata.json",
    "validate": "opentool validate --input tools"
  },
  "dependencies": {
    "@msgpack/msgpack": "^3.0.0",
    "@noble/hashes": "^1.5.0",
    "@turnkey/sdk-server": "^1.2.3",
    "@turnkey/viem": "^0.4.3",
    "opentool": "0.7.13",
    "viem": "^2.9.8"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
